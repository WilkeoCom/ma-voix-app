import 'package:charts_flutter/flutter.dart';

/// Linear data type.
class LinearPolls {
  final String legend;
  final int votesNumber;
  final Color color;

  LinearPolls(this.legend, this.votesNumber, this.color);
}
